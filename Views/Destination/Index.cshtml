@{
  Layout = "_Layout";
}

@using TravelApiMVC.Models;
@model List<TravelApiMVC.Models.Destination>;

<h1>Destinations!</h1>

@if (@Model.Count == 0)
{
  <h3>No destinations have been added yet!</h3>
} 

@foreach (Destination destination in Model)
{
  <ul>
    <li>Country: @destination.Country</li>
    <li>City: @Html.ActionLink($"{destination.City}", "Details", new { id = destination.DestinationId })</li>
    <li>Rating: @destination.Rating</li>
  </ul>
}

 <button class="btn btn-outline-success"><p>@Html.ActionLink("➕Add a new destination", "Create", "Destination")</p></button>